- name: install different packages
  host: frontend
  tasks:
  - name: installing mysql, nodejs
    ansible.builtin.dnf:
      name: {{ item }}
      state: present
    loop:
    - mysql
    - nodejs
    - nginx
    - zip